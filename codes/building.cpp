#include "building.h"
//_decOrInc:     0:decrease   1:increase
//_code: illustrates the code of each product
//_number: shows the number of each item to increase(buy) or decrease(sell).
void building::transfer(bool _decOrInc, int _code, int _number){}
int building::getLvl() { return lvl; }
int building::getCapasity() { return capasity; }
int building::getNumberOfProducts() { return numberOfProducts; }
void building::upgrade(int code) {}
void building::check() {}
